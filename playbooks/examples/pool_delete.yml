---
- hosts: localhost
  tasks:
    - name: delete pool
      pool:
        login:
          host: 192.168.70.218
          username: admin
          password: Password123!
        delete:
          id: "{{ delete_id }}"

      register: result
    - debug: var=result
