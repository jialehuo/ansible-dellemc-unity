---
- hosts: localhost
  tasks:
    - name: Pool Creation and Updates
      pool:
        login:
          host: "{{ host }}"
          username: "{{ username }}"
          password: "{{ password }}"
        createVSA:
          name: swimmingPool
          addPoolUnitParameters: [{"poolUnit" : {"id" : "vdisk_1"}}, {"poolUnit" : {"id" : "vdisk_2"}}]
      register: result
    - debug: var=result